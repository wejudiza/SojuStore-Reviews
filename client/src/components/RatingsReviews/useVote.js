import { useState } from 'react';

export default function useVote(initalValues) {
  const [values, setValues] = useState(initalValues);

  return [
    // ...values,
    // (e) => setValues({

    // })
  ];
}
