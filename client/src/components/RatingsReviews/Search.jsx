import React, { useState } from 'react';

// only start searching when # chars > 3
// clear search when chars < 3
// apply filter to allReviews in addition to any other filters

// onchange handler on char count
// onchange handler for text
// save both as state
// apply keyword search to review body wen char count > 3
// set allReviers state
// regex?


// const convertDate = (date) => {
//   const pattern = /\d{4}-\d{2}-\d{2}/;
//   const oldDate = date.match(pattern)[0];
//   const newDate = dt(oldDate, "YYYY-MM-DD").format("MMMM DD, YYYY");
//   return newDate;
// };

export default function Search(props) {
  return (
    <div>

    </div>
  )
}
